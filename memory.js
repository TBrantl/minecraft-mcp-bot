// --- Memory Module ---

function addSemanticFact(agentState, fact, value) {
  // ... (move logic from index.js here)
}

function getSemanticFact(agentState, fact) {
  // ... (move logic from index.js here)
}

function reflectAndLearn(agentState) {
  // ... (move logic from index.js here)
}

function updateWorldModel(bot, agentState) {
  // ... (move logic from index.js here)
}

// Export memory functions
module.exports = {
  addSemanticFact,
  getSemanticFact,
  reflectAndLearn,
  updateWorldModel
}; 